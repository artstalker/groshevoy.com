@using groshevoy.com
@using groshevoy.com.Core.Objects

@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">
        @ActionLinkExtensions.TagLink(htmlHelper, tag)
    </div>
  }
}